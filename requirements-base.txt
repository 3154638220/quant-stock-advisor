# 与 Jetson / 桌面共用：不含 PyTorch（aarch64 须用 NVIDIA 官方 wheel，见 environment.yml / bootstrap 脚本）
# Python：见 .python-version 与 pyproject.toml（当前锁定 3.10.x）

akshare>=1.14.0
duckdb>=1.0.0
pyarrow>=14.0.0
polars>=1.0.0
pandas>=2.0.0
# Jetson 官方 PyTorch wheel 与 NumPy 2.x 二进制不兼容，须 <2（见 bootstrap 后 import torch 警告）
numpy>=1.26.1,<2.0.0
pyyaml>=6.0
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
scipy>=1.11.0
tqdm>=4.66.0
requests>=2.31.0
