[pytest]
testpaths = tests
# CI 中强制覆盖率门槛（Phase B: 45%→55%, 目标 Phase C: 70%）
# 本地开发可注释 --cov-fail-under 行以跳过覆盖率门槛
addopts = -q --cov=src --cov-fail-under=45 --cov-report=term-missing
# 核心 pipeline 模块目标覆盖率 ≥ 80%
